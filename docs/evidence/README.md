# Evidence artifacts

`manifest.json` is generated by `scripts/build_evidence_report.py`. It is safe to review and attach only after the operator confirms its contents remain sanitized. Live captures and media are not stored here by default.
